/*<ul>
<li> {child template} </li>
.
.
.
<li> {child template} </li>

</ul>
*/

// quid best approach ? what do you do in your loop ? 
// you create <li>{child template}</li> that you clone 
// you create <li> and clone the template object 
// or your create a li and you create a new template directly from the input parameter 
function list() {

  return 
}